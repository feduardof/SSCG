version: '3'
services:
    openssl:
        build:
            dockerfile: ./dockerfile
            context: .

        image: eduardo/openssl
        container_name: openssl
        volumes:
          - ./data:/data
